spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop

  data:
    redis:
      host: localhost
      port: 6379
      repositories:
        enabled: false
  cache:
    type: none

  ai:
    openai:
      api-key: mock-test-key-12345
      chat:
        options:
          model: gpt-3.5-turbo
      image:
        enabled: false
    ollama:
      chat:
        enabled: false
      embedding:
        enabled: false


management:
  health:
    redis:
      enabled: false

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000

